# Demo mode is the safe default. Live mode fails closed unless both server-only
# settings below are supplied by a trusted deployment environment.
SETU_DATA_MODE=demo
SETU_BACKEND_URL=
SETU_BACKEND_API_KEY=
